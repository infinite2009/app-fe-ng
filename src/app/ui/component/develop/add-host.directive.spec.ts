import { AddHostDirective } from './add-host.directive';

describe('AddHostDirective', () => {
  it('should create an instance', () => {
    const directive = new AddHostDirective(null);
    expect(directive).toBeTruthy();
  });
});
